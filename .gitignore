# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.production
.env.*.local
# Backups of the above — a stray `git add -A` on the VPS would otherwise
# stage production Supabase and Stripe keys.
.env*.backup*
.env*.bak

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI local state (not committed)
supabase/.temp/

# Deployment artifact (has hardcoded env values, kept local)
coming-soon.html

# Claude Code per-machine permission allowlist. Maps local paths, so it stays
# out of the public repo. Note: the `*.local` rule above does NOT cover this,
# since the filename ends in .json.
.claude/settings.local.json

# Scratch directory for drafts and working files
Temp/